import { ReactNode } from '../../../../../node_modules/.pnpm/react@19.2.8/node_modules/react';
import { TextControlSize } from '../textControl';
import { SelectItem } from './selectOptions';
export type ComboboxSize = TextControlSize;
export type ComboboxProps = {
    options: readonly SelectItem[];
    value?: string;
    defaultValue?: string;
    onValueChange?: (value: string) => void;
    name?: string;
    form?: string;
    placeholder?: string;
    emptyMessage?: ReactNode;
    size?: ComboboxSize;
    label?: ReactNode;
    helper?: ReactNode;
    error?: ReactNode;
    invalid?: boolean;
    valid?: boolean;
    disabled?: boolean;
    required?: boolean;
    fullWidth?: boolean;
    className?: string;
    id?: string;
    'aria-label'?: string;
    'aria-labelledby'?: string;
    'aria-describedby'?: string;
};
export declare function Combobox({ options, value: valueProp, defaultValue, onValueChange, name, form, placeholder, emptyMessage, size, label, helper, error, invalid, valid, disabled, required, fullWidth, className, id, 'aria-label': ariaLabel, 'aria-labelledby': ariaLabelledBy, 'aria-describedby': ariaDescribedBy, }: ComboboxProps): import("react").JSX.Element;
