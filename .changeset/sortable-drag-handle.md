---
'd-ui': minor
---

Add `SortableList`, `SortableItem`, and `DragHandle` (dnd-kit, ADR 0001): pointer, touch, and keyboard reordering with live-region announcements (`messages`, English fallback), `orientation` vertical / horizontal / grid, `as="ol"`, locked items, and `moveSortableItem` for the non-drag alternative required by WCAG 2.2 2.5.7.
