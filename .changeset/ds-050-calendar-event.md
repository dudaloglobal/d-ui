---
'd-ui-education': minor
---

Add `CalendarEvent` (DS-050) in the new `d-ui-education` package: an academic session item (title, course, location, start/end, type) coloured by type through `--d-ui-edu-event-<type>` tokens that fall back to `d-ui` semantic tokens, with day and week compositions over `Calendar` in Storybook.
