---
'd-ui': minor
---

Split Tag (labelled status pill, TagGroup, controlled dismiss) from Badge (count/dot overlay on a host). `<Badge>Nouveau</Badge>` is no longer valid — use `Tag`. A coloured span on its own is not a design-system component.
