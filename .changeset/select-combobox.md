---
'd-ui': minor
---

Add Select and Combobox with shared options (groups, disabled items, descriptions), Combobox multiple chips, listStatus, filter modes, custom rendering, keyboard typeahead/filter, and native `name`/`form` submit via a visually hidden `<select>`.
