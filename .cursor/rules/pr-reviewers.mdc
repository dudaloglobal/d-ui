---
description: Always assign the PR author and request Angelo-Cosme and fruitizz as reviewers
alwaysApply: true
---

# PR reviewers

When creating or updating a GitHub pull request:

- Assign the PR author.
- Request reviewers `Angelo-Cosme` and `fruitizz` (skip if that person is the author).
- Automation lives in `.github/workflows/pr-assignment.yml`; still set `--assignee` / `--reviewer` on `gh pr create`.
- Put `Closes #N` or `Related to #N` in the PR body and run `.github/scripts/sync-project-status.sh <pr-number>` so the project board moves (see `project-status.mdc`).
- Never add Co-authored-by or tooling attribution.
